import clsx from 'clsx';
import s from './HomePage.module.css';

const HomePage = () => {
  return (
    // <div className={clsx(s.home)}>
    <div className={clsx(s.image_container)}>test</div>
    // </div>
  );
};

export default HomePage;
